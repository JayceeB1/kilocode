#!/usr/bin/env sh
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" != "local-supervisor" ]; then
  echo "❌ Commit refusé: branche '$branch'. Utiliser uniquement 'local-supervisor'."
  exit 1
fi
exit 0